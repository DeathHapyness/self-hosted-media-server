services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    restart: unless-stopped

    ports:
      - "8082:80"

    volumes:
      - /mnt/media:/srv
      - ./database:/database
      - ./config:/config
